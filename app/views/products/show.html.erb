<h1><%= @product.name %></h1>

<table class="table">
  <thead>
    <tr>
      <th scope="col">stock left</th>
      <th scope="col">product image</th>
      <th scope="col">description</th>
      <th scope="col">price</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= @product.stock %> left!</td>
      <td><%= image_tag @product.image.thumb('300x200#').url if @product.image_stored? %></td>
      <td><%= @product.description %></td>
      <td>£<%= @product.price %></td>
    </tr>
  </tbody>
</table>
