<h1 class="text-info"><%= @product.name %></h1>
<hr class="my-6">

<table class="table">
  <thead>
    <tr>
      <th scope="col">price</th>
      <th scope="col">product image</th>
      <th scope="col">description</th>
      <th scope="col">stock left</th>
      <th scope="col">options</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>£<%= @product.price %></td>
      <td><%= image_tag @product.image.thumb('250x150#').url if @product.image_stored? %></td>
      <td><%= @product.description %></td>
      <td><%= @product.stock %> left!</td>
      <td>
        <%= form_tag basket_items_path do %>
        <%= submit_tag 'add to cart' %>
        <%= hidden_field(:basket_item, :product_id, value: @product.id) %>
        <%end%>
      </td>
    </tr>
  </tbody>
</table>
